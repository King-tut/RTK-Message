import { Box } from '@mui/material'
import React, {useEffect} from 'react'
import { useSelector } from 'react-redux'


const MessageArea = () => {
 //const dms = useSelector((state) => state.persistedReducer.allOfYourMessages?.allOfYourMessages)
   //const data = dms
   //console.log(`${dms}`)

   
  return (
    
    <Box>
      Message Area
    </Box>
    
  )
}

export default MessageArea